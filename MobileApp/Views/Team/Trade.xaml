<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MobileApp.Views.Team.Trade"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MobileApp.Models.Team"
             x:DataType="viewmodels:TradeViewModel"
             Title="Trueque">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                BackgroundColor="#007bff" 
                Padding="15,10">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                <Button Grid.Column="0"
                        Text="← Volver"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        FontSize="14"
                        Command="{Binding ReturnCommand}"
                        HorizontalOptions="Start" />

                <Label Grid.Column="1" 
                       Text="{Binding TradePartnerName}" 
                       FontSize="18" 
                       FontAttributes="Bold"
                       TextColor="White"
                       VerticalOptions="Center"
                       HorizontalOptions="Center" />

                <Button Grid.Column="2"
                        Text="Cambiar"
                        BackgroundColor="Transparent"
                        TextColor="White"
                        FontSize="12"
                        Command="{Binding SelectFranchiseCommand}"
                        HorizontalOptions="End" />
            </Grid>
        </Border>

        <Border Grid.Row="1" 
                BackgroundColor="White" 
                Padding="15,10"
                Stroke="#dee2e6"
                StrokeThickness="1">
            <StackLayout Spacing="8">
                <Label Text="Balance del Trueque" 
                       FontSize="14" 
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />

                <Grid ColumnDefinitions="*,Auto,*" ColumnSpacing="10">
                    <Label Grid.Column="0" 
                           Text="{Binding UserTotalValueText}"
                           FontSize="12"
                           TextColor="#007bff"
                           HorizontalOptions="Start" />

                    <Label Grid.Column="1"
                           Text="vs"
                           FontSize="12"
                           TextColor="#6c757d"
                           HorizontalOptions="Center" />

                    <Label Grid.Column="2"
                           Text="{Binding FranchiseTotalValueText}"
                           FontSize="12" 
                           TextColor="#dc3545"
                           HorizontalOptions="End" />
                </Grid>

                <Border BackgroundColor="#f8f9fa"
                        HeightRequest="20"
                        StrokeShape="RoundRectangle 10">
                    <Grid>
                        <Border BackgroundColor="#007bff"
                                WidthRequest="{Binding UserValueBarWidth}"
                                HorizontalOptions="Start"
                                StrokeShape="RoundRectangle 10" />
                        <Border BackgroundColor="#dc3545"
                                WidthRequest="{Binding FranchiseValueBarWidth}"
                                HorizontalOptions="End"
                                StrokeShape="RoundRectangle 10" />
                        <Label Text="{Binding TradeBalanceText}"
                               FontSize="10"
                               TextColor="White"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Grid>
                </Border>
            </StackLayout>
        </Border>

        <Grid Grid.Row="2" RowDefinitions="Auto,*">
            <StackLayout Grid.Row="0"
                     Orientation="Horizontal" 
                     BackgroundColor="#f8f9fa"
                     Spacing="0">
                <Button Text="Resumen"
                        BackgroundColor="{Binding SummaryTabColor}"
                        TextColor="{Binding SummaryTabTextColor}"
                        FontSize="14"
                        Padding="20,10"
                        Command="{Binding ShowSummaryTabCommand}"
                        HorizontalOptions="Fill" />
                <Button Text="Tus Items"
                        BackgroundColor="{Binding UserTabColor}"
                        TextColor="{Binding UserTabTextColor}"
                        FontSize="14"
                        Padding="20,10"
                        Command="{Binding ShowUserTabCommand}"
                        HorizontalOptions="Fill" />
                <Button Text="Sus Items"
                        BackgroundColor="{Binding FranchiseTabColor}"
                        TextColor="{Binding FranchiseTabTextColor}"
                        FontSize="14"
                        Padding="20,10"
                        Command="{Binding ShowFranchiseTabCommand}"
                        HorizontalOptions="Fill" />
            </StackLayout>

            <ScrollView Grid.Row="1" BackgroundColor="White">
                <StackLayout>
                    <StackLayout IsVisible="{Binding IsSummaryTabVisible}" Padding="15" Spacing="20">

                        <Border BackgroundColor="#f8f9fa"
                                Stroke="#007bff"
                                StrokeThickness="1"
                                Padding="15"
                                StrokeShape="RoundRectangle 8">
                            <StackLayout Spacing="10">
                                <Label Text="Estás Ofreciendo:" 
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#007bff" />
                                <CollectionView ItemsSource="{Binding SelectedUserItems}">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="viewmodels:TradeItemSummary">
                                            <Label Text="{Binding DisplayText}"
                                                   FontSize="14"
                                                   Padding="5,2" />
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </StackLayout>
                        </Border>

                        <Border BackgroundColor="#f8f9fa"
                                Stroke="#dc3545"
                                StrokeThickness="1"
                                Padding="15"
                                StrokeShape="RoundRectangle 8">
                            <StackLayout Spacing="10">
                                <Label Text="Ellos Ofrecen:" 
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#dc3545" />
                                <CollectionView ItemsSource="{Binding SelectedFranchiseItems}">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate x:DataType="viewmodels:TradeItemSummary">
                                            <Label Text="{Binding DisplayText}"
                                                   FontSize="14"
                                                   Padding="5,2" />
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </StackLayout>
                        </Border>
                    </StackLayout>

                    <StackLayout IsVisible="{Binding IsUserTabVisible}"
                                 Padding="15"
                                 Spacing="15">

                        <StackLayout IsVisible="{Binding HasUserPicks}">
                            <Label Text="Tus Picks del Draft" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   TextColor="#495057" />
                            <CollectionView ItemsSource="{Binding UserPicks}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewmodels:TradePickItem">
                                        <Border BackgroundColor="{Binding SelectionColor}"
                                                Stroke="#dee2e6"
                                                StrokeThickness="1"
                                                Padding="12"
                                                Margin="0,2"
                                                StrokeShape="RoundRectangle 6">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TradeViewModel}}, Path=TogglePickSelectionCommand}"
                                                                      CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                                <Label Grid.Column="0"
                                                       Text="{Binding DisplayText}" 
                                                       FontSize="14"
                                                       FontAttributes="Bold"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="1"
                                                       Text="{Binding ValueText}"
                                                       FontSize="12"
                                                       TextColor="#007bff"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="2"
                                                       Text="{Binding SelectionIcon}"
                                                       FontSize="16"
                                                       TextColor="{Binding SelectionIconColor}"
                                                       VerticalOptions="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </StackLayout>

                        <StackLayout IsVisible="{Binding HasUserPlayers}">
                            <Label Text="Tus Jugadores" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   TextColor="#495057" />
                            <CollectionView ItemsSource="{Binding UserPlayers}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewmodels:TradePlayerItem">
                                        <Border BackgroundColor="{Binding SelectionColor}"
                                                Stroke="#dee2e6"
                                                StrokeThickness="1"
                                                Padding="12"
                                                Margin="0,2"
                                                StrokeShape="RoundRectangle 6">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TradeViewModel}}, Path=TogglePlayerSelectionCommand}"
                                                                      CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                                <StackLayout Grid.Column="0" Spacing="2">
                                                    <Label Text="{Binding Name}" 
                                                           FontSize="14"
                                                           FontAttributes="Bold" />
                                                    <Label Text="{Binding Position}" 
                                                           FontSize="12"
                                                           TextColor="#6c757d" />
                                                </StackLayout>
                                                <Label Grid.Column="1"
                                                       Text="{Binding ValueText}"
                                                       FontSize="12"
                                                       TextColor="#007bff"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="2"
                                                       Text="{Binding SelectionIcon}"
                                                       FontSize="16"
                                                       TextColor="{Binding SelectionIconColor}"
                                                       VerticalOptions="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </StackLayout>
                    </StackLayout>

                    <StackLayout IsVisible="{Binding IsFranchiseTabVisible}"
                                 Padding="15"
                                 Spacing="15">

                        <StackLayout IsVisible="{Binding HasFranchisePicks}">
                            <Label Text="Sus Picks del Draft" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   TextColor="#495057" />
                            <CollectionView ItemsSource="{Binding FranchisePicks}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewmodels:TradePickItem">
                                        <Border BackgroundColor="{Binding SelectionColor}"
                                                Stroke="#dee2e6"
                                                StrokeThickness="1"
                                                Padding="12"
                                                Margin="0,2"
                                                StrokeShape="RoundRectangle 6">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TradeViewModel}}, Path=TogglePickSelectionCommand}"
                                                                      CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                                <Label Grid.Column="0"
                                                       Text="{Binding DisplayText}" 
                                                       FontSize="14"
                                                       FontAttributes="Bold"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="1"
                                                       Text="{Binding ValueText}"
                                                       FontSize="12"
                                                       TextColor="#dc3545"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="2"
                                                       Text="{Binding SelectionIcon}"
                                                       FontSize="16"
                                                       TextColor="{Binding SelectionIconColor}"
                                                       VerticalOptions="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </StackLayout>

                        <StackLayout IsVisible="{Binding HasFranchisePlayers}">
                            <Label Text="Sus Jugadores" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   TextColor="#495057" />
                            <CollectionView ItemsSource="{Binding FranchisePlayers}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate x:DataType="viewmodels:TradePlayerItem">
                                        <Border BackgroundColor="{Binding SelectionColor}"
                                                Stroke="#dee2e6"
                                                StrokeThickness="1"
                                                Padding="12"
                                                Margin="0,2"
                                                StrokeShape="RoundRectangle 6">
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:TradeViewModel}}, Path=TogglePlayerSelectionCommand}"
                                                                      CommandParameter="{Binding .}" />
                                            </Border.GestureRecognizers>
                                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="10">
                                                <StackLayout Grid.Column="0" Spacing="2">
                                                    <Label Text="{Binding Name}" 
                                                           FontSize="14"
                                                           FontAttributes="Bold" />
                                                    <Label Text="{Binding Position}" 
                                                           FontSize="12"
                                                           TextColor="#6c757d" />
                                                </StackLayout>
                                                <Label Grid.Column="1"
                                                       Text="{Binding ValueText}"
                                                       FontSize="12"
                                                       TextColor="#dc3545"
                                                       VerticalOptions="Center" />
                                                <Label Grid.Column="2"
                                                       Text="{Binding SelectionIcon}"
                                                       FontSize="16"
                                                       TextColor="{Binding SelectionIconColor}"
                                                       VerticalOptions="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </StackLayout>
                    </StackLayout>
                </StackLayout>
            </ScrollView>
        </Grid>

        <Border Grid.Row="3" 
                BackgroundColor="White"
                Stroke="#dee2e6"
                StrokeThickness="1"
                Padding="15,10">
            <StackLayout Orientation="Horizontal" 
                         Spacing="10">
                <Button Text="Cancelar"
                        BackgroundColor="#6c757d"
                        TextColor="White"
                        FontSize="14"
                        Padding="15,8"
                        Command="{Binding CancelTradeCommand}"
                        HorizontalOptions="Fill" />
                <Button Text="Forzar Trueque"
                        BackgroundColor="#ffc107"
                        TextColor="Black"
                        FontSize="14"
                        Padding="15,8"
                        Command="{Binding ForceTradeCommand}"
                        IsVisible="{Binding CanTrade}"
                        HorizontalOptions="Fill" />
                <Button Text="Solicitar Trueque"
                        BackgroundColor="#28a745"
                        TextColor="White"
                        FontSize="14"
                        Padding="15,8"
                        Command="{Binding ProposeTradeCommand}"
                        IsEnabled="{Binding CanTrade}"
                        HorizontalOptions="Fill" />
            </StackLayout>
        </Border>

        <StackLayout Grid.Row="0" Grid.RowSpan="4"
                     BackgroundColor="#80000000"
                     IsVisible="{Binding IsLoading}"
                     VerticalOptions="Fill"
                     HorizontalOptions="Fill">
            <ActivityIndicator IsRunning="True" 
                               Color="White" 
                               VerticalOptions="Center" />
            <Label Text="{Binding LoadingMessage}"
                   TextColor="White"
                   FontSize="16"
                   HorizontalOptions="Center" />
        </StackLayout>
    </Grid>
</ContentPage>