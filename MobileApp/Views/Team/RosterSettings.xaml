<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MobileApp.Views.Team.RosterSettings"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MobileApp.Models.Team"
             x:DataType="viewmodels:RosterSettingsViewModel"
             Title="Ajustes de Plantilla">

    <ScrollView>
        <StackLayout Padding="20" Spacing="20">

            <StackLayout IsVisible="{Binding IsLoading}" Spacing="15">
                <ActivityIndicator IsRunning="True" Color="#007bff" />
                <Label Text="Cargando Ajustes de Plantilla..." 
                       HorizontalOptions="Center" 
                       FontSize="16" 
                       TextColor="#6c757d" />
            </StackLayout>

            <StackLayout IsVisible="{Binding IsNotLoading}" Spacing="20">

                <Border BackgroundColor="#007bff" 
                        Padding="20"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 12">
                    <Grid RowDefinitions="Auto,Auto" RowSpacing="10">
                        <Label Grid.Row="0"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center">
                            <Label.Text>
                                <MultiBinding StringFormat="Ajustes de Plantilla: {0}">
                                    <Binding Path="TeamName" />
                                </MultiBinding>
                            </Label.Text>
                        </Label>

                        <Label Grid.Row="1"
                               Text="{Binding Team.UserUsername}" 
                               TextColor="White" 
                               FontSize="14"
                               HorizontalOptions="Center"
                               Opacity="0.8" />
                    </Grid>
                </Border>

                <Border BackgroundColor="White"
                        Stroke="#dee2e6"
                        StrokeThickness="1"
                        Padding="20"
                        StrokeShape="RoundRectangle 12">
                    <StackLayout Spacing="20">
                        <Label Text="⚙️ Ajustes" 
                               FontSize="18" 
                               FontAttributes="Bold" 
                               TextColor="#28a745" />

                        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="15">
                            <StackLayout  Grid.Column="0"
                                        Spacing="8">
                                <Label Text="💰 Límite Salarial (%)" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="#333" 
                                       HorizontalOptions="Center" />
                                <Stepper Value="{Binding RosterSettingsCap}" 
                                         Minimum="60" 
                                         Maximum="120" 
                                         Increment="1" />
                                <Label Text="{Binding RosterSettingsCap, StringFormat='{0}%'}" 
                                       HorizontalOptions="Center" 
                                       FontSize="16" 
                                       FontAttributes="Bold" 
                                       TextColor="#007bff" />
                                <Label Text="Porcentaje del Límite Salarial Actual" 
                                       FontSize="11" 
                                       TextColor="#6c757d" 
                                       HorizontalOptions="Center" />
                            </StackLayout>

                            <StackLayout  Grid.Column="1" 
                                        Spacing="8">
                                <Label Text="👥 Máx Jugadores Por Equipo" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="#333" 
                                       HorizontalOptions="Center" />
                                <Stepper Value="{Binding RosterSettingsMaxPerTeam}" 
                                         Minimum="1" 
                                         Maximum="10" 
                                         Increment="1" />
                                <Label Text="{Binding RosterSettingsMaxPerTeam}" 
                                       HorizontalOptions="Center" 
                                       FontSize="16" 
                                       FontAttributes="Bold" 
                                       TextColor="#007bff" />
                                <Label Text="Máx jugadores por cada franquicia" 
                                       FontSize="11" 
                                       TextColor="#6c757d" 
                                       HorizontalOptions="Center" />
                            </StackLayout>

                            <StackLayout Grid.Column="2" 
                                        Spacing="8">
                                <Label Text="🛡️ Jugadores Protegidos de cada Franquicia" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="#333" 
                                       HorizontalOptions="Center" />
                                <Stepper Value="{Binding RosterSettingsProtectedPerTeam}" 
                                         Minimum="0" 
                                         Maximum="5" 
                                         Increment="1" />
                                <Label Text="{Binding RosterSettingsProtectedPerTeam}" 
                                       HorizontalOptions="Center" 
                                       FontSize="16" 
                                       FontAttributes="Bold" 
                                       TextColor="#007bff" />
                                <Label Text="Jugadores a proteger por cada Franquicia" 
                                       FontSize="11" 
                                       TextColor="#6c757d" 
                                       HorizontalOptions="Center" />
                            </StackLayout>
                        </Grid>
                    </StackLayout>
                </Border>

                <Border BackgroundColor="White"
                        Stroke="#dee2e6"
                        StrokeThickness="1"
                        Padding="20"
                        StrokeShape="RoundRectangle 12">
                    <StackLayout Spacing="20">
                        <Label Text="🛡️ Selección de Jugadores Protegidos" 
                               FontSize="18" 
                               FontAttributes="Bold" 
                               TextColor="#17a2b8" />

                        <StackLayout Spacing="10">
                            <Label Text="Método de Selección" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="#333" />

                            <StackLayout Orientation="Horizontal" Spacing="20">
                                <RadioButton Content="🤖 Automático" 
                                             IsChecked="{Binding IsAutomaticProtection}" 
                                             FontSize="14" />
                                <RadioButton Content="✋ Manual" 
                                             IsChecked="{Binding IsManualProtection}" 
                                             FontSize="14" />
                            </StackLayout>

                            <Label IsVisible="{Binding IsAutomaticProtection}"
                                   Text="Jugadores protegidos por defecto" 
                                   FontSize="11" 
                                   TextColor="#6c757d" />
                            <Label IsVisible="{Binding IsManualProtection}"
                                   Text="Selección manual de jugadores protegidos" 
                                   FontSize="11" 
                                   TextColor="#6c757d" />
                        </StackLayout>

                        <StackLayout IsVisible="{Binding IsManualProtection}" Spacing="10">
                            <Label Text="Progresión de Protecciones" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="#333" />

                            <Border BackgroundColor="#f8f9fa" 
                                    Padding="15" 
                                    Stroke="#dee2e6" 
                                    StrokeThickness="1" 
                                    StrokeShape="RoundRectangle 8">
                                <StackLayout Spacing="10">
                                    <Label FontSize="12" TextColor="#6c757d">
                                        <Label.Text>
                                            <MultiBinding StringFormat="{}{0} de 32 franquicias">
                                                <Binding Path="TeamsWithSelections" />
                                            </MultiBinding>
                                        </Label.Text>
                                    </Label>

                                    <ProgressBar Progress="{Binding SelectionProgress}" 
                                                 ProgressColor="#28a745" 
                                                 BackgroundColor="#e9ecef" />

                                    <Label FontSize="12" TextColor="#6c757d">
                                        <Label.Text>
                                            <MultiBinding StringFormat="{}{0} jugadores protegidos">
                                                <Binding Path="TotalProtectedPlayers" />
                                            </MultiBinding>
                                        </Label.Text>
                                    </Label>
                                </StackLayout>
                            </Border>

                            <Button Text="📋 Selecciona Jugadores Protegidos" 
                                    Command="{Binding DisplayPlayerSelectionCommand}"
                                    BackgroundColor="#17a2b8"
                                    TextColor="White"
                                    FontSize="14"
                                    Padding="12" />
                        </StackLayout>
                    </StackLayout>
                </Border>

                <StackLayout IsVisible="{Binding ShowPlayerSelection}" Spacing="15">

                    <Border BackgroundColor="White"
                            Stroke="#ffc107"
                            StrokeThickness="2"
                            Padding="15"
                            StrokeShape="RoundRectangle 8">
                        <StackLayout Spacing="10">
                            <Label Text="🏈 Seleccione Franquicia" 
                                   FontSize="16" 
                                   FontAttributes="Bold" 
                                   TextColor="#333" />

                            <Picker ItemsSource="{Binding NflTeams}"
                                    ItemDisplayBinding="{Binding DisplayName}"
                                    SelectedItem="{Binding SelectedNflTeam}"
                                    Title="Choose a team..." />

                            <Label IsVisible="{Binding HasSelectedTeam}" 
                                   FontSize="12" 
                                   TextColor="#6c757d">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0}/{1} jugadores protegidos">
                                        <Binding Path="CurrentTeamProtectedCount" />
                                        <Binding Path="RosterSettingsProtectedPerTeam" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>
                        </StackLayout>
                    </Border>

                    <StackLayout IsVisible="{Binding HasSelectedTeam}" Spacing="10">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                            <Label Grid.Column="0" 
                                   Text="{Binding SelectedNflTeam.Name}" 
                                   FontSize="16" 
                                   FontAttributes="Bold" 
                                   VerticalOptions="Center" />

                            <Button Grid.Column="1" 
                                    Text="Clear All" 
                                    Command="{Binding ClearTeamSelectionCommand}"
                                    BackgroundColor="#dc3545"
                                    TextColor="White"
                                    FontSize="12"
                                    Padding="8" />
                        </Grid>

                        <CollectionView ItemsSource="{Binding TeamPlayersGrouped}"
                                        IsGrouped="True"
                                        BackgroundColor="Transparent">
                            <CollectionView.GroupHeaderTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding Key}" 
                                           FontSize="14" 
                                           FontAttributes="Bold" 
                                           TextColor="#6c757d" 
                                           Padding="10,15,10,5" />
                                </DataTemplate>
                            </CollectionView.GroupHeaderTemplate>

                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:ProtectedPlayerViewModel">
                                    <Border BackgroundColor="White" 
                                            Stroke="#dee2e6" 
                                            StrokeThickness="1" 
                                            Padding="15" 
                                            Margin="5"
                                            StrokeShape="RoundRectangle 8">
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RosterSettingsViewModel}}, Path=TogglePlayerProtectionCommand}"
                                                                  CommandParameter="{Binding}" />
                                        </Border.GestureRecognizers>

                                        <Border.Triggers>
                                            <DataTrigger TargetType="Border" Binding="{Binding IsProtected}" Value="True">
                                                <Setter Property="Stroke" Value="#28a745" />
                                                <Setter Property="BackgroundColor" Value="#f8fff9" />
                                            </DataTrigger>
                                        </Border.Triggers>

                                        <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="10">
                                            <StackLayout Grid.Column="0">
                                                <Label Text="{Binding Player.Name}" 
                                                       FontSize="14" 
                                                       FontAttributes="Bold" />
                                                <Label Text="{Binding Player.Position}" 
                                                       FontSize="11" 
                                                       TextColor="#007bff" />
                                            </StackLayout>

                                            <Label Grid.Column="1" 
                                                   Text="{Binding Player.APY}" 
                                                   FontSize="12" 
                                                   FontAttributes="Bold" 
                                                   TextColor="#28a745" 
                                                   VerticalOptions="Center" />

                                            <Label Grid.Column="2" 
                                                   Text="⭐" 
                                                   FontSize="14" 
                                                   TextColor="#ffc107" 
                                                   VerticalOptions="Center"
                                                   IsVisible="{Binding Player.DefaultProtected}" />

                                            <Label Grid.Column="3" 
                                                   Text="✅" 
                                                   FontSize="16" 
                                                   TextColor="#28a745" 
                                                   VerticalOptions="Center"
                                                   IsVisible="{Binding IsProtected}" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <Button Text="✅ Selección Finalizada" 
                                Command="{Binding HidePlayerSelectionCommand}"
                                BackgroundColor="#28a745"
                                TextColor="White"
                                FontSize="14"
                                Padding="12" />
                    </StackLayout>
                </StackLayout>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                    <Button Grid.Column="0"
                            Text="⬅️ Volver al Equipo"
                            Command="{Binding GoBackToTeamCommand}"
                            BackgroundColor="Transparent"
                            TextColor="#6c757d"
                            BorderColor="#6c757d"
                            BorderWidth="1"
                            FontSize="14"
                            Padding="12" />

                    <Button Grid.Column="1"
                            Text="💾 Guardar Ajustes"
                            Command="{Binding SaveSettingsCommand}"
                            BackgroundColor="#28a745"
                            TextColor="White"
                            FontSize="14"
                            Padding="12"
                            IsEnabled="{Binding CanSave}" />
                </Grid>

            </StackLayout>

        </StackLayout>
    </ScrollView>
</ContentPage>