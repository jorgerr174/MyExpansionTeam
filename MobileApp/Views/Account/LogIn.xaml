<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="MobileApp.Views.Account.LogIn"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MobileApp.Models.Account"
             x:DataType="viewmodels:LogInViewModel"
             Title="LogIn">

    <ScrollView>
        <StackLayout Padding="20" Spacing="20">

            <!-- Title - same as your H2 in Razor -->
            <Label Text="LogIn" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <!-- Identifier Input - Direct translation of @Html.TextBoxFor(m => m.Identifier) -->
            <StackLayout>
                <Label Text="Identifier" />
                <Entry Text="{Binding Identifier}" 
                       Placeholder="Username or Email"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" />
            </StackLayout>

            <!-- Password Input - Direct translation of @Html.PasswordFor(m => m.Password) -->
            <StackLayout>
                <Label Text="Password" />
                <Entry Text="{Binding Password}" 
                       IsPassword="True"
                       Placeholder="Password"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" />
            </StackLayout>

            <!-- Error Message - Translation of ValidationSummary -->
            <Label Text="{Binding ErrorMessage}" 
                   TextColor="Red" 
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}" />

            <!-- Login Button - Direct translation of your submit button -->
            <Button Text="LogIn" 
                    Command="{Binding LoginCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                    BackgroundColor="Blue"
                    TextColor="White" />

            <!-- Loading Indicator -->
            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                             IsVisible="{Binding IsLoading}" />

            <!-- Go Back - Translation of your asp-controller="Home" link -->
            <Button Text="Go Back" 
                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.GoBackCommand}"
                    BackgroundColor="Transparent"
                    TextColor="Blue" />

        </StackLayout>
    </ScrollView>
</ContentPage>